{
  "dependencies": {
    "axios": "^1.9.0",
    "express": "^5.1.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "validate:schema": "ajv validate -s schema/programme.schema.json -d data/*.json"
  }
}
